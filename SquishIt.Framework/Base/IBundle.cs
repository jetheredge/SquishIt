namespace SquishIt.Framework.Base
{
    /*
    public interface IBundle<T> where T : class, IBundle<T>
    {
        T Add(string filePath);
        T Add(string filePath, string group);
        T Add(string filePath, int order);
        T Add(string filePath, string group, int order);
        T AddRemote(string localPath, string remotePath);
        T AddEmbeddedResource(string localPath, string embeddedResourcePath);
        
        T AlwaysRender();
        T HashKeyNamed(string hashQueryStringKeyName);
        T ForceDebug();
        T ForceRelease();

        string Render(string renderTo);
        string RenderNamed(string name);
        string RenderCached(string name);
        void AsNamed(string name, string renderTo);
        string AsCached(string name, string filePath);
        void ClearCache();

        T WithAttribute(string name, string value);
        T WithAttributes(System.Collections.Generic.Dictionary<string, string> attributes, bool merge = true);
        T WithGroupAttribute(string name, string value, string group);
        T WithGroupAttributes(System.Collections.Generic.Dictionary<string, string> attributes, string group, bool merge = true);
        ////virtual T WithMinifier<T>() where T : Minifiers.IMinifier<T>;
    }
     */
}